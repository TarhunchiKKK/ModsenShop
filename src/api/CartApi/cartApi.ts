import {
    useGetCartsQuery,
    useLazyGetCartsQuery,
    useCreateCartMutation,
    useRemoveCartMutation,
} from "./hooks";

export const cartApi = {
    useGetCartsQuery,
    useLazyGetCartsQuery,
    useCreateCartMutation,
    useRemoveCartMutation,
};
